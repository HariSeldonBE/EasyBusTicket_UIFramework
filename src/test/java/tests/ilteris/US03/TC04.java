package tests.ilteris.US03;

import org.testng.annotations.Test;
import org.testng.asserts.SoftAssert;
import pages.EasyBusTicketPage;
import utilities.ConfigReader;
import utilities.Driver;

public class TC04 {

    @Test
    public void homePageBannerKismiBastanSonaGorunurlukTesti(){
/*
        //https://easybusticket.com/ anasayfasina gidin
        Driver.getDriver().get(ConfigReader.getProperty("eBTUrl"));

        SoftAssert softAssert = new SoftAssert();
        EasyBusTicketPage easyBusTicketPage = new EasyBusTicketPage();

        String expectedURl="https://easybusticket.com/";
        String actualUrl=Driver.getDriver().getCurrentUrl();
        softAssert.assertEquals(actualUrl,expectedURl);

        softAssert.assertTrue(easyBusTicketPage.inJust3SimpleStepsGetYourBusTicketText.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.lookingForABusLogo.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.selectYourTicketLogo.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.payYourBillLogo.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.offeredFacilitiesText.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.wifiLogo.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.pillowLogo.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.waterLogo.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.teaAndCoffeLogo.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.ourTestimonialsTitle.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.recentBlogPostTitle.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.blogPostImgOne.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.blogPostImgTwo.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.blogPostImgThree.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.postContentOne.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.postContentTwo.isDisplayed());
        softAssert.assertTrue(easyBusTicketPage.postContentThree.isDisplayed());


        softAssert.assertAll();

        Driver.closeDriver();


 */
    }
}
